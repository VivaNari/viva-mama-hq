# Git
.git
.gitignore

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual Environments
venv/
.venv/
venv_mcp/

# Local configuration
.env
.vscode/

# Project Specific
# NOTE: .local_vector_store/ is intentionally NOT ignored — CI produces the
# prebuilt FAISS index there and the runtime stage COPYs it into the image.
# Documents must never enter the image build context (they live in GCS only):
data/
data_new/
*.ipynb
*.zip
*.csv
*.xlsx
docs/

# Logs
*.log
