# VivaMama RAG chatbot — runtime dependencies (used by pip / the Docker image).
# The canonical dev manifest is pyproject.toml (managed with uv); keep this file
# in sync via:  uv export --no-dev --format requirements-txt > requirements.txt

python-dotenv
pydantic
pydantic-settings
structlog
redis

# LangChain core + community integrations + Groq
langchain==1.0.3
langchain-core
langchain-community
langchain-groq
# Vertex AI (Gemini) — default LLM provider; Groq kept as an env-flag fallback.
langchain-google-vertexai
jinja2

# Embeddings + local vector store
sentence-transformers
faiss-cpu

# Downloads the embedding model from GCS at container boot (model is no longer
# baked into the runtime image; see app/rag/model_provisioner.py).
google-cloud-storage

# (Optional) If you later use FastAPI service:
# fastapi
# uvicorn[standard]

# MCP SDK (adapter is optional; included for future)
mcp

# API + data access
pymongo==4.6.1
fastapi
uvicorn[standard]
slowapi
chardet
pypdf
