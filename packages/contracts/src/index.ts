/**
 * @vivamama/contracts — shared API contract and domain types.
 *
 * Consumed by:
 *   - services/backend  (route definitions, DTOs)
 *   - apps/mobile       (API client, dashboard types)
 *   - services/chatbot  (backend tool call URLs)
 */
export * from './endpoints';
export * from './domain';
