import { StyleSheet } from 'react-native';

export const onboardingStyles = StyleSheet.create({
  welcomeText: {
    fontSize: 36,
    fontWeight: 600,
  },
  welcomeTextCaption: {
    fontSize: 15,
    fontWeight: 200,
  },
});
