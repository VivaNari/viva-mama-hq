/**
 * VivaMama's WhatsApp Business number, with country code and no "+" — the format
 * wa.me expects. This is the same line that sends OTPs; patients use it to reschedule
 * or cancel a booking, since neither is possible in-app yet.
 */
export const VIVAMAMA_SUPPORT_WHATSAPP = "919599120017";
